let cadena = "caracter";
let char='a';

let buscar=(cualquierCadena,cualquierCaracter)=>{
    let arreglo=new Array(2);
    arreglo[0]=cualquierCadena.indexOf(cualquierCaracter);
    arreglo[1]=cualquierCadenacadena.lastIndexOf(cualquierCaracter);
    return console.log(arreglo);   
}

buscar(cadena,char);
